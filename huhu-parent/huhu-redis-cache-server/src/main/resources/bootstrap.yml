eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
spring:
  application:
    name: redis-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      label: master
      name: redis-server