server:
  port: 9100
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JiavLee/huhuConfigMSG
#          username: 不需要账号和密码了
#          password:  不需要
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/